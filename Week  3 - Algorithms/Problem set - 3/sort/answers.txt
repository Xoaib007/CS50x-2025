sort1 uses: Bubble

How do you know?: There's a huge difference between upper bound and lower bound, just like bubble sort. O(n²) and Ω(n). also the upper bound was slower and lower bound was faster. which indicates it must be bubble sort.

sort2 uses: Merges

How do you know?: Upper bound and slower both both were similar and faster than others

sort3 uses: Selection

How do you know?: Upper bound and lower bound both were kinda similar and slower than others
