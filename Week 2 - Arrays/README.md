###
## CLI argument:
` clang -o hello hello.c` instead of `make hello` for more efficiency

` clang -o hello hello.c -lcs50` to import 3rd party library, like cs50 library. Math and stdio comes with c initially, so no need to import them.

## Code processing
- Preprocessing
- compiling
- assembling
- linking
